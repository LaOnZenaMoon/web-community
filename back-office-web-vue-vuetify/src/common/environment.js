const ENV_LOCAL = 'local';
const ENV_DEV = 'dev';
const ENV_PROD = 'prod';

const CURRENT_ENV = ENV_LOCAL;
const LOZM_GATEWAY_SERVER = 'http://127.0.0.1:8882';

export {
  ENV_LOCAL,
  ENV_DEV,
  ENV_PROD,
  CURRENT_ENV,
  LOZM_GATEWAY_SERVER,
};
